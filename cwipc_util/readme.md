# cwipc_util - Core implementation of cwipc

This subtree has the core implementation (pointcloud objects, capturer objects, auxiliary things like logging and error handling). In addition it contains
some functionality that is not really core but did not fit anywhere else (filtering, synthetic pointcloud generator).
